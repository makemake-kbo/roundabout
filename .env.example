# Roundabout Configuration Example
# Copy this file to .env and customize as needed

# ============================================================================
# Collection Settings
# ============================================================================

# How often to run collection cycles (in seconds)
# 0 = run once and exit
# 45 = run every 45 seconds (recommended)
COLLECTION_INTERVAL=45

# Maximum number of concurrent API requests
CONCURRENCY=10

# HTTP request timeout in seconds
REQUEST_TIMEOUT=10

# Number of retry attempts for failed requests
RETRIES=1

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# ============================================================================
# ClickHouse Configuration
# ============================================================================

# ClickHouse HTTP interface URL
CLICKHOUSE_URL=http://localhost:8123

# ClickHouse database name
CLICKHOUSE_DB=roundabout

# ClickHouse authentication (leave empty for no auth)
CLICKHOUSE_USER=
CLICKHOUSE_PASSWORD=

# Number of records to batch before writing to ClickHouse
CLICKHOUSE_BATCH_SIZE=2000

# ClickHouse request timeout in seconds
CLICKHOUSE_TIMEOUT=10

# ============================================================================
# Grafana Configuration
# ============================================================================

# Grafana admin credentials (change these for production!)
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# ============================================================================
# Optional: Stop Filtering
# ============================================================================

# Comma-separated list of stop codes to collect (leave empty for all)
# STOP_CODES=1001,1002,1003

# Maximum number of stops to process per cycle (leave empty for all)
# LIMIT=100

# ============================================================================
# Optional: Priority Routes
# ============================================================================

# Comma-separated list of route short names to prioritize
# When set, only stops served by these routes will be collected
# Example: PRIORITY_ROUTES=7,84,E2
PRIORITY_ROUTES=7,83,21,21L,29,5,6,95,27

# ============================================================================
# Optional: Geographic Bounding Box
# ============================================================================

# Filter stops by geographic bounds (Belgrade approximate defaults shown)
# All four values must be set to enable bounding box filtering
BBOX_MIN_LAT=44.79
BBOX_MAX_LAT=44.83
BBOX_MIN_LON=20.42
BBOX_MAX_LON=20.52
